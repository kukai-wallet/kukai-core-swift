// swift-tools-version:5.5
// This is a dummy/empty Package declartion to trigger an undocumented Xcode "feature" of hiding a directory from the navigator when imported, so various assets aren't pickuped up and made available when they shouldn't be

import PackageDescription

let package = Package()
