name: Unit Test

# Run unit tests on every push or pull request, to main or develop
on:
  push:
    branches:
      - main
      - develop
  pull_request:
    branches:
      - main
      - develop

# Checkout the code, and run mxcl's xcodebuild action to run the unit tests
jobs:
  build:
    runs-on: macOS-latest
    strategy:
      matrix:
        platform:
          - iOS
    steps:
      - uses: actions/checkout@v2.3.4

      - name: Test
        run: xcodebuild -scheme KukaiCoreSwift test -sdk iphonesimulator -destination 'platform=iOS Simulator,name=iPhone 12' -enableCodeCoverage YES -resultBundlePath "xcode-$GITHUB_RUN_ID.xcresult"
      
      - name: Upload results
        uses: actions/upload-artifact@v2
        with:
          name: "results-$GITHUB_RUN_ID.xcresult"
          path: "/Users/runner/work/kukai-core-swift/kukai-core-swift/xcode-$GITHUB_RUN_ID.xcresult"


      # Issue using the wrong scheme coming from a dependency
      #- uses: mxcl/xcodebuild@v1.6.0
      #  with:
      #    platform: ${{ matrix.platform }}
      #    action: test
      #    code-coverage: true
      #    upload-logs: always